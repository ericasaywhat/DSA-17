import java.util.HashMap;

public class SplitCoins {

    public static int splitCoins(int[] coins) {
        HashMap<Integer, Integer> memo = new HashMap<>();   //coins placed to minimum difference
        return splitCoinsHelper(coins, memo);
    }

    private static int splitCoinsHelper(int[] coins, HashMap<Integer,Integer> memo){
        if(coins.length == 0) return memo[];

        return 0;
    }


        private long recursiveFunction(/* Only arguments that are part of subproblem */){

            // BASE CASES


            /** HAS THIS BEEN MEMOIZED? */


            // RECURRENCE RELATION


            /** UPDATE THE MEMO */


            // RETURN THE ANSWER

            return 0;

        }
    }

